import { SplitFNamePipe } from './split-fname.pipe';

describe('SplitFNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SplitFNamePipe();
    expect(pipe).toBeTruthy();
  });
});
